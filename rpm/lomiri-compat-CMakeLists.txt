set(LOMIRI_COMPAT_INSTALL_DIR ${DATA_DIR}/qml/compat)
set(LOMIRI_COMPAT_SOURCE_DIR ${PROJECT_SOURCE_DIR}/lomiri-compat/qml)

set(LOMIRI_COMPAT_FILES
        ${LOMIRI_COMPAT_SOURCE_DIR}/MainView.qml
        ${LOMIRI_COMPAT_SOURCE_DIR}/UbuUnits.qml
)

install(FILES ${LOMIRI_COMPAT_FILES} DESTINATION ${LOMIRI_COMPAT_INSTALL_DIR})

